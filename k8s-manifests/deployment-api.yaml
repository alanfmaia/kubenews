apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-web-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: deploy-web-api
  template:
    metadata:
      labels:
        app: deploy-web-api
    spec:
      containers:
      - name: web-api
        image: afmaia/kube-news:v1
        ports:
        - containerPort: 8080
        env:
        - name: DB_DATABASE
          value: "kubenews"
        - name: DB_USERNAME
          value: "kubenews"
        - name: DB_PASSWORD
          value: "Pg#123"
        - name: DB_HOST
          value: "postgres-service"
        